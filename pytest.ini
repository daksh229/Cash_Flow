[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    integration: multi-module tests that touch the DB
    regression: metric-drift checks against baselines.yml
